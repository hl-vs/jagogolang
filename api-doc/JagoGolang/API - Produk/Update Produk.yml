info:
  name: Update Produk
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseURL}}/api/product/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Indomie Rebus",
        "price": 2500,
        "stock": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
