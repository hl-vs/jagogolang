info:
  name: Update Produk
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseURL}}/api/produk/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nama": "Indomie Rebus",
        "harga": 2500,
        "stok": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
