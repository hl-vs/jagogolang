info:
  name: Get Produk by Name
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/api/product?name=indo"
  params:
    - name: name
      value: indo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
