info:
  name: Add Produk
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/product"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": 1,
        "name": "asasasa",
        "price": 10,
        "stock": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
