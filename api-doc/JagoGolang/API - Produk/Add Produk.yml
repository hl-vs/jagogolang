info:
  name: Add Produk
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/product"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Gorengan",
        "price": 2000,
        "stock": 1,
        "category_id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
