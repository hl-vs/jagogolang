info:
  name: Get Produk
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/product"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
