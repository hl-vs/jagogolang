info:
  name: Get Produk by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/product/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
