info:
  name: Delete Category
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/api/category/1"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
