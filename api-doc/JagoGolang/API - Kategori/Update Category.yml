info:
  name: Update Category
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseURL}}/api/category/32"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Cemilan",
        "description": "Kategori Cemilan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
