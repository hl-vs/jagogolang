info:
  name: Get Category
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/category/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
