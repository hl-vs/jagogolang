info:
  name: Get Category by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/category/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
