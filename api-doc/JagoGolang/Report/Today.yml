info:
  name: Today
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/api/report/hari-ini"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
